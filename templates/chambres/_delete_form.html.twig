<form method="post" action="{{ path('app_chambres_delete', {'id': chambre.id}) }}" onsubmit="return confirm('Voulez vous supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chambre.id) }}">
    <button class="btn btn-danger btn-sm" class="btn">Supprimer</button>
</form>
