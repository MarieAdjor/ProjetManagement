{% extends 'base.html.twig' %}

{% block title %}Factures!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


   
    
<div class="container-fluid">
 <h1>Liste Generale des factures </h1>

    Nb: Fonctionnalité d'impression pour plus de details non disponible pour l'instant.
		<div class="row page-titles">
			<ol class="breadcrumb">
				<li class="breadcrumb-item active">
					<a href="javascript:void(0)">LISTE</a>
				</li>
				<li class="breadcrumb-item">
					<a href="javascript:void(0)">Facture</a>
				</li>
			</ol>
		</div>
		<!-- row -->
		<div class="row">
			<div class="">
				<div class="card">
					<div class="card-header">
						<h2 class="card-title">Liste des Factures.</h2>
					</div>
					<div class="card-body">
						<div class="input-primary">
							<table class="table">
								<thead>
									<tr>
										<th>Id</th>
										<th>Nom Complet</th>
										<th>Chambre</th>
										<th>Duree</th>
                                        <th>Date Debut</th>
										<th>Frais</th>
									</tr>
								</thead>
								<tbody>
									{% for item in facture %}
                                        <td>{{item.id}}</td>
                                        <td>{{item.client.nomComplet}}</td>
                                        <td>{{item.chambre.libelle}}</td>
                                        <td>{{item.duree}}</td>
                                        <td>{{item.datedebut}}</td>
                                        <td>{{(item.chambre.prix*item.duree)}}</td>
                                    {% endfor %}

								</tbody>
							</td>
						</tr>
				</tbody>
			</table>

		</div>
	</div>


{% endblock %}
