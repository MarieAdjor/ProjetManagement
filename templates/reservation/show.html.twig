{% extends 'base.html.twig' %}

{% block title %}Reservation
{% endblock %}

{% block body %}
	<div class="container-fluid">
		<div class="row page-titles">
			<ol class="breadcrumb">
				<li class="breadcrumb-item active">
					<a href="javascript:void(0)">RESERVATION</a>
				</li>
				<li class="breadcrumb-item">
					<a href="javascript:void(0)">Details</a>
				</li>
			</ol>
		</div>
		<!-- row -->
		<div class="row">
			<div class="col-xl-8 col-lg-8">
				<div class="card">
					<div class="card-header">
						<h2 class="card-title">information de la Reservation</h2>
					</div>
					<div class="card-body">
						<div class="input-primary">
							<table class="table">
								<tbody>
									<tr>
										<th>Id</th>
										<td>{{ reservation.id }}</td>
									</tr>
									<tr>
										<th>Duree</th>
										<td>{{ reservation.duree }} jours</td>
									</tr>
									<tr>
										<th>Date_debut</th>
										<td>{{ reservation.dateDebut }}</td>
									</tr>
								</tbody>
							</table>
							<div class="row">
								<div class="col-3">
									<a class="btn btn-warning btn-sm" href="{{ path('app_reservation_index') }}">Revenir à la Liste</a>

								</div>
								<div class="col-2">
									<a class="btn btn-info btn-sm" href="{{ path('app_reservation_edit', {'id': reservation.id}) }}">Editer</a>

								</div>
								<div class="col-2">
									{{ include('reservation/_delete_form.html.twig') }}

								</div>

							</div>
						</div>
					</div>

				</div>


				.....................
				
			{% endblock %}
